package tokenring.messageprocessor;

import tokenring.Message;
import tokenring.Node;

public class VoidMessageProcessor implements MessageProcessor {
    public void processMessage(Node node, Message message) {
    }
}
